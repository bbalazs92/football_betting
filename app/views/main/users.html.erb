<h1>List of registered users</h1>

<ul>
  <% @users.each do |user| %>
    <li>
      <%= user.id %>. <%= user.first_name %> <%= user.last_name %> | <%= user.email %> | <%= user.points_overall %> 
    </li>
  <% end %>
  <%= link_to "Home", root_path %>
</ul>